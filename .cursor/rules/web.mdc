# PC 端 AI 编码规范（Next.js）

## 目录结构

```
apps/web/src/
├── app/                  # Next.js App Router
│   ├── (auth)/           # 认证相关页面（登录/注册）
│   ├── (dashboard)/      # 登录后页面
│   └── layout.tsx
├── components/           # PC 端专用组件
│   └── {ComponentName}/
│       ├── index.tsx
│       └── index.module.scss
├── hooks/                # 自定义 Hook
├── stores/               # 状态管理（Zustand）
├── services/             # 调用 packages/api，做业务编排
└── utils/                # PC 端专用工具
```

## 规范

### 组件

- 使用函数式组件 + Hooks，禁止 Class 组件
- Server Component 优先，需要交互时才用 `'use client'`
- 样式用 CSS Modules（`.module.scss`）

### 状态管理

- 使用 Zustand
- Store 文件放 `stores/{domain}.store.ts`
- Server Component 不使用 store，直接 fetch

### API 调用

- 统一从 `@spec-coding/api` 导入调用函数
- Server Component 中直接调用（无需 useEffect）
- Client Component 中通过 store action 调用
- 禁止在组件中硬编码 URL

### 认证

- 登录流程：账号密码 → 调用后端 `/web/auth/login` → 拿 JWT
- JWT 存储在 `httpOnly cookie`（安全）
- Next.js middleware 拦截未登录请求，重定向到 `/login`

## 禁止行为

- 禁止在 Client Component 中直接 fetch（走 store）
- 禁止在 page 里写超过 150 行（提取到组件/hook）
- 禁止魔法字符串（用 `@spec-coding/constants`）
- 禁止 `any` 类型

